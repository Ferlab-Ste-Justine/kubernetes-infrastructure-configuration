%{ for secret in secrets ~}
%{ if index(secrets, secret) > 0 ~}
---
%{ endif ~}
apiVersion: v1
kind: Secret
metadata:
  name: ${secret.name}
type: Opaque
data:
%{ for key,val in secret.attributes ~}
  ${key}: ${base64encode(val)}
%{ endfor ~}
%{ endfor ~}